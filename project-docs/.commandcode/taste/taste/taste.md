# Taste

- Treats the written "Coding Standards & Engineering Rules" document (strict TypeScript incl. `noUncheckedIndexedAccess`, no `any`/`@ts-ignore`, no inline CSS, no business logic in UI components, no direct DB access from UI, server-side authorization, DRY/reusable components, OpenAPI contract-first APIs, Zod validation, tests, no new libraries when the existing stack suffices) as mandatory project standards and expects the codebase to comply, not just be documented. Confidence: 0.95
- Wants mock/static/hardcoded data removed from UI pages and replaced with real database-backed data, converting screens flow-by-flow rather than deleting the mock module early. Confidence: 0.95
- Expects professional, production-grade ("senior coder") implementations, not demo/toy work — calls out childish-quality output explicitly. Confidence: 0.8
- Wants to reuse the existing Supabase PostgreSQL database and env vars (`DATABASE_URL`/`DIRECT_URL`) exactly as-is; explicitly forbids creating a new database, resetting data, switching providers, or inventing credentials. Confidence: 0.9
- Treats Supabase as the PostgreSQL host accessed through Prisma with the existing connection (not Supabase Auth/SSR scaffolding) — kept the existing `User`/`ApiToken` models and built custom session auth on top. Confidence: 0.7
- Keeps secrets server-side: `.env` never committed, server-only env vars never exposed in browser bundles; only `NEXT_PUBLIC_*` may reach client code; when an API key is provided, updates it in `.env` for both client and server vars and flags previously-committed keys for revocation. Confidence: 0.85
- Wants prerequisites verified first (database connection, Prisma/schema validity, env config) before wiring features or migrating data. Confidence: 0.8
- Wants a step-by-step implementation plan produced after review, before starting feature work. Confidence: 0.75
- Requests full-project audits covering API flows, page wiring, and static/hardcoded UI elements — not just isolated fixes; e.g., asked to "check full project" and to review the codebase against the project's coding standards and report what's wrong before any work begins. Confidence: 0.85
- Deploys to Vercel and wants local secrets and local database artifacts (`.env`, `prisma/*.db`, `.next`, `node_modules`) excluded from Git and deployment uploads. Confidence: 0.7
- When offered deployment / hosting choices, prefers to keep the existing Vercel setup unchanged rather than introduce a new host, service, or provider — even when that means replacing a third-party library (e.g., Socket.IO) with a hand-rolled in-framework solution (raw `ws` over `experimental_upgradeWebSocket`) to fit Vercel's constraints. Explicit constraint: "do not change anything on Vercel." Confidence: 0.9
- Wants the existing project structure/routing inspected before refactoring, then implementation done using the framework's idiomatic pattern already in use (e.g., Next.js App Router route groups for a shared header/footer layout) rather than introducing new patterns or libraries; explicitly forbids building a parallel implementation when functionality already exists — existing components, utilities, API patterns, validation, database models, and auth conventions must be reused. Confidence: 0.85
- Wants refactors to preserve the current UI/design exactly — reuse existing components and styling, no visual changes — and to remove duplication (e.g., per-page header/footer) rather than restyle. Confidence: 0.85
- Wants persistent shared dashboard/portal layouts: the sidebar/header shell should render once and persist across client-side navigation (only the main content area changes), with the active sidebar item derived from the current route — explicitly asks that the shell not be reloaded, recreated, or duplicated on every page. Confidence: 0.8
- Treats lint compliance (`npm run lint` / eslint with prettier rules) as a hard requirement enforced by GitHub Actions CI — flags CI failures as blockers and expects committed code to pass lint clean, including prettier formatting (no formatting drift like stray indentation). Confidence: 0.8
- Wants CMS content managed separately per role/audience (e.g., a client home block vs a professional home block, each with its own text and data) rather than one shared content set that edits both sides at once. Confidence: 0.85
- For marketing/landing components such as job or professional cards, wants CMS-editable mock data (with a default mock fallback when none is set) instead of live database-backed data, so a single CMS edit updates every card site-wide without touching the database. Confidence: 0.8
- Prefers a focused CMS/admin experience: intermediate page-list navigation (e.g., a "Website pages / Live pages" list in the CMS) should be dropped when there's only one meaningful editor, going straight to the relevant content editor rather than a generic management shell. Confidence: 0.55
- Communicates UI feedback by attaching screenshots of the offending screen and expects the fix to match what the screenshot shows (e.g., removed the flagged "Live pages" list entirely; flagged the form-like CMS editor in a screenshot). Confidence: 0.7
- Provides screenshots of a working reference/example (e.g., a Google Maps-style address search) as the spec for what to build and expects the implemented feature to look and behave like the shown example. Confidence: 0.75
- Wants feature parity across equivalent pages/flows: when a feature exists on one page (e.g., map address search on the post job page), it should be added to all related pages (find professional page, find job page) using the same pattern — not left as a one-page-only feature. Confidence: 0.85
- Prefers CMS editing to happen on a live, full-page preview of the actual page (an iframe canvas rendering the real homepage, edit-in-place) rather than a form-based editor with inputs — expects the editing route to hide the admin sidebar/header so the editor renders full-width and looks like the real page, not like a form. Confidence: 0.5 (later reversed — user asked for all of the CMS editor changes to be deleted outright)
- Expects admin routes that are dedicated to a full-page experience (e.g., the CMS editor) to escape the standard admin shell — the admin sidebar and header should be hidden on those routes so the editor fills the viewport like a real page. Confidence: 0.5 (later reversed — user asked for all of the CMS editor changes to be deleted outright)
- When the user says to "delete/revert all changes," expects a clean, surgical git-based revert limited to exactly the current task's changes: inspect `git status`/`git log` first to separate committed work from uncommitted work, revert only the task files (and remove new files), leave previously-committed work (e.g., an earlier layout refactor) untouched unless explicitly included, clean up any stray artifacts, and verify the working tree state before summarizing what was reverted vs. kept. Confidence: 0.85
- Wants role-specific landing pages after login: professionals should land on the CMS-editable professional home page (`/professional-home`), not the portal dashboard — explicitly requested ("when i login as a prof so prof home page do this") after earlier choosing the dashboard; expects all entry points (login redirect, OAuth, email-verify, logo click, header Home link, and a server-side redirect from `/` for logged-in pros) to consistently route professionals to their home page. Confidence: 0.85
- When the user reports that they cannot edit content on the CMS page (e.g., "in cms page how it work in that page i cant change"), treats it as a functional blocker — expects the CMS editing flow to actually work end-to-end in the browser for that page and wants a plain explanation of how editing works there, not just a description of the underlying architecture. Reported again ("in this page i cant change"), with the root cause being page text keys missing from the editor's defaults so edits were silently dropped. Confidence: 0.7

le marketing pages; CMS marketing pages are for public viewing/content editing, not as authenticated landing pages. Confidence: 0.7

- When reviewing or delivering API code (e.g., "check client job api"), explicitly expects clean, well-organized, "perfect" output rather than messy/ugly code — flags messy formatting or disorganized structure when asking for API inspection. Confidence: 0.6
- Treats the database schema (Prisma `schema.prisma`) as in scope for coding-standards review: when asking to "check" the project against standards, expects the Prisma schema itself to be reviewed and brought into conformance, not just application code. Confidence: 0.7
- Writes in informal, non-native English with frequent typos and dropped words (e.g., "becus", "somthing", "withaout", "show" instead of "so"/"something"/"without"/"so"); expects the assistant to understand the intent without strict grammar and to mirror the same casual register rather than over-formalizing. Confidence: 0.85
- Often sends bare, two-word directives like "do it" / "do this" / "do that" as the entire message after a multi-option question, deferring the choice to the assistant — expects the assistant to pick a sensible option and proceed rather than ask another clarifying question. Confidence: 0.85
- Wants screens to reflect server-side changes live without a manual page reload — expects realtime subscriptions (Socket.IO or equivalent) to be wired into the affected screens (project tracking, dashboards, my-jobs) so the counterparty's open tab and counters update in place; treats "I have to reload to see it" as a bug. Willing to swap the realtime transport (e.g., Socket.IO → raw `ws`) if the original doesn't fit the deployment platform, as long as the live-update behavior is preserved end-to-end. Confidence: 0.85

rmatting or disorganized structure when asking for API inspection. Confidence: 0.6
