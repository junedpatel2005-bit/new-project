"use client";
import { useEffect,useState } from "react";
export function AdminDataPage({resource,title,description}:{resource:string;title:string;description:string}){const[data,setData]=useState<any>(null);useEffect(()=>{void fetch(`/api/admin/data/${resource}`,{cache:"no-store"}).then(r=>r.json()).then(setData)},[resource]);const rows: any[] = data ? Object.entries(data).flatMap(([,v])=>Array.isArray(v)?v:[]) : [];return <div><p className="text-xs font-bold uppercase tracking-[.2em] text-indigo-400">Admin module</p><h1 className="mt-2 font-display text-3xl font-bold">{title}</h1><p className="mt-2 text-slate-400">{description}</p><section className="mt-8 overflow-hidden rounded-2xl border border-white/10 bg-white/[.035]"><div className="border-b border-white/10 px-5 py-4 text-sm font-semibold text-slate-300">Live database records · {rows.length}</div>{!data?<div className="h-52 animate-pulse bg-white/5"/>:rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-white/[.03] text-xs uppercase tracking-wide text-slate-400"><tr>{Object.keys(rows[0]).slice(0,6).map(k=><th key={k} className="px-5 py-3 font-medium">{k}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={row.id??i} className="border-t border-white/10 text-slate-300">{Object.keys(rows[0]).slice(0,6).map(k=><td key={k} className="max-w-56 truncate px-5 py-4">{typeof row[k]==="object"?JSON.stringify(row[k]):String(row[k]??"—")}</td>)}</tr>)}</tbody></table></div>:<p className="p-8 text-sm text-slate-400">No records found.</p>}</section></div>}
