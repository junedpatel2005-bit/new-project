import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { db } from "@/lib/db";
import { sessionCookie, verifySession } from "@/lib/auth";

async function sessionFrom(request: NextRequest) { const token = request.cookies.get(sessionCookie)?.value; if (!token) return null; try { return await verifySession(token); } catch { return null; } }
export async function GET(request: NextRequest) { const session = await sessionFrom(request); if (!session) return NextResponse.json({ error: "Sign-in required." }, { status: 401 }); const wallet = await db.wallet.upsert({ where: { userId: session.userId }, update: {}, create: { userId: session.userId } }); const earned = session.role === "PROFESSIONAL" ? await db.projectTransaction.aggregate({ where: { professionalId: session.userId, status: "COMPLETED" }, _sum: { amount: true } }) : await db.projectTransaction.aggregate({ where: { clientId: session.userId, status: "COMPLETED" }, _sum: { amount: true } }); const withdrawals = session.role === "PROFESSIONAL" ? await db.projectWithdrawal.aggregate({ where: { professionalId: session.userId, status: { in: ["PENDING", "COMPLETED"] } }, _sum: { amount: true } }) : null; const withdrawalHistory = session.role === "PROFESSIONAL" ? await db.projectWithdrawal.findMany({ where: { professionalId: session.userId }, orderBy: { createdAt: "desc" }, take: 10 }) : []; const available = session.role === "PROFESSIONAL" ? Math.max(0, (earned._sum.amount ?? 0) - (withdrawals?._sum.amount ?? 0)) : 0; return NextResponse.json({ wallet, total: earned._sum.amount ?? 0, available, reserved: withdrawals?._sum.amount ?? 0, withdrawals: withdrawalHistory }); }
export async function POST(request: NextRequest) { const session = await sessionFrom(request); if (!session || session.role !== "PROFESSIONAL") return NextResponse.json({ error: "Professional access required." }, { status: 403 }); const parsed = z.object({ amount: z.number().int().positive(), destinationLabel: z.string().trim().min(2).max(120) }).safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "Enter a valid withdrawal amount and payout destination." }, { status: 400 }); const earned = await db.projectTransaction.aggregate({ where: { professionalId: session.userId, status: "COMPLETED" }, _sum: { amount: true } }); const reserved = await db.projectWithdrawal.aggregate({ where: { professionalId: session.userId, status: { in: ["PENDING", "COMPLETED"] } }, _sum: { amount: true } }); const available = Math.max(0, (earned._sum.amount ?? 0) - (reserved._sum.amount ?? 0)); if (parsed.data.amount > available) return NextResponse.json({ error: "Withdrawal amount exceeds your available balance." }, { status: 400 }); const withdrawal = await db.projectWithdrawal.create({ data: { professionalId: session.userId, amount: parsed.data.amount, destinationType: "BANK", destinationLabel: parsed.data.destinationLabel, status: "PENDING" } }); return NextResponse.json({ withdrawal }, { status: 201 }); }
