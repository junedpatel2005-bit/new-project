"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { LockKeyhole, ShieldCheck } from "lucide-react";
export default function AdminLogin(){const router=useRouter();const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[pending,setPending]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setPending(true);setError("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username,password})});const d=await r.json();setPending(false);if(!r.ok)return setError(d.error??"Unable to sign in.");router.replace("/admin");}return <main className="min-h-screen bg-[#070b16] p-5 text-slate-100"><div className="mx-auto grid min-h-[calc(100vh-2.5rem)] max-w-5xl overflow-hidden rounded-3xl border border-white/10 bg-[#10182b] shadow-2xl lg:grid-cols-[1.1fr_.9fr]"><section className="hidden bg-[radial-gradient(circle_at_top_right,#4f46e5,transparent_45%),linear-gradient(145deg,#111c39,#0d1325)] p-10 lg:block"><ShieldCheck className="h-10 w-10 text-indigo-300"/><p className="mt-24 text-xs font-bold uppercase tracking-[.25em] text-indigo-300">Servio internal</p><h1 className="mt-4 font-display text-4xl font-bold leading-tight">Marketplace<br/>control center.</h1><p className="mt-5 max-w-sm text-sm leading-6 text-slate-400">Secure administration for users, verification, operations, finance, and CMS content.</p></section><section className="flex items-center p-7 sm:p-12"><div className="w-full"><div className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-500 text-white"><LockKeyhole className="h-5 w-5"/></div><p className="mt-8 text-xs font-bold uppercase tracking-[.2em] text-indigo-400">Administrator sign in</p><h2 className="mt-2 font-display text-3xl font-bold">Welcome back</h2><p className="mt-2 text-sm text-slate-400">Use your private administrator credentials.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm font-medium">Admin ID<input autoFocus value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-[#070b16] px-3 text-sm outline-none focus:border-indigo-400" placeholder="Administrator ID"/></label><label className="block text-sm font-medium">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-[#070b16] px-3 text-sm outline-none focus:border-indigo-400" placeholder="Password"/></label>{error&&<p className="text-sm text-rose-400">{error}</p>}<button disabled={pending} className="h-11 w-full rounded-xl bg-indigo-500 text-sm font-semibold text-white transition hover:bg-indigo-400 disabled:opacity-60">{pending?"Signing in…":"Open admin portal"}</button></form></div></section></div></main>}
